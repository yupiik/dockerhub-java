name: DockerHub CI

on:
  push:
    tags:
      - '*/*.*.*'

jobs:
  docker:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v1

      - name: Parse Tag Name
        uses: jungwinter/split@v2
        id: parsed_tag
        with:
          msg: ${{ github.ref }}
          separator: '/'

      - name: Log parsed tag
        run: |
          echo ${{ github.ref }}
          echo ossyupiik/${{ steps.parsed_tag.outputs._2 }}:${{ steps.parsed_tag.outputs._3 }}

      - name: Login to DockerHub
        uses: docker/login-action@v1
        if: startsWith( github.ref, 'refs/tags/')
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Create release
        id: create_release
        uses: actions/create-release@v1
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          tag_name: ${{ github.ref }}
          release_name: Release ${{ github.ref }}

      - name: Build and push
        id: docker_build
        uses: docker/build-push-action@v2
        if: startsWith( github.ref, 'refs/tags/')
        with:
          push: true
          context: ./${{ steps.parsed_tag.outputs._2 }}
          file: ./Dockerfile
          tags: ossyupiik/${{ steps.parsed_tag.outputs._2 }}:${{ steps.parsed_tag.outputs._3 }}
          labels: |
            org.opencontainers.image.vendor=Yupiik

      - name: Image Report
        run: echo ossyupiik/${{ steps.parsed_tag.outputs._2 }}:${{ steps.parsed_tag.outputs._3 }}@${{ steps.docker_build.outputs.digest }}
